# Libraries for code interpretation and execution
litellm>=1.83.0
litellm[proxy]
Pygments 
pyreadline3 
python-dotenv 
pytest
# TOML model registry (configs/models.toml). tomllib is stdlib on 3.11+;
# tomli is the same parser backported for Python 3.10 (still CI-pinned).
tomli; python_version < "3.11"

# Libraries for making HTTP requests
Requests 
rich

# Libraries for data analysis
pandas
matplotlib
plotly
openpyxl
tabulate
pyarrow
# Optional: DuckDB accelerates /sql over local files
duckdb
# Scientific computing (#223)
seaborn
scipy
scikit-learn
statsmodels
reportlab
numpy

# Libraries for standard libries 
stdlib_list

# Legacy libraries
legacy-cgi
# Optional: web search tool (--search). DuckDuckGo needs no API key.
# ddgs is the actively-maintained successor to duckduckgo-search (which
# still installs but silently returns empty results); keep both so
# environments pinned to the old name still resolve a working import.
ddgs>=9.14.4
duckduckgo-search>=6.0.0


